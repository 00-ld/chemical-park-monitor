# Docker 环境配置 - 覆盖默认 application.yml
spring:
  datasource:
    url: jdbc:mysql://mysql:3306/chemical?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: ${SPRING_DATASOURCE_PASSWORD}

server:
  port: 8081
