# GitHub

This directory contains repository automation such as CI workflows and GitHub configuration.

Rules:
- CI checks should run repository hygiene audits before merge.
- Do not store deployment secrets in workflow files.
- Use repository or environment secrets for sensitive values.
